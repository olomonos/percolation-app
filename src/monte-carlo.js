// import percolator-class

function threshold(lattice) {
    let itPercolates = percolator.isPercolate(lattice);
    while (!itPercolates) {
        // open one more cell
    }
    // The fraction of sites that are opened when the system percolates 
    // provides an estimate of the percolation threshold. 
}

// run threshold T times
// get average result
